<div class="container">
  <h1>{{product_type}}</h1>
    <div class="site-section mt-5">
    <div class="container">
      <div class="row">
        {{#each prodList}}
          <div class="col-lg-4 mb-5 col-md-6 product">
            <div class="wine_v_1 text-center pb-4">
              <a href="/detail" id="show-detail" class="thumbnail d-block mb-4">
                <img src="{{this.image_link}}" alt="{{this.name}}" class="img-fluid">
              </a>
              <div class="wine-actions">
                <h3 class="heading-2 product-text" style="font-size:18px"><a href="">{{this.name}}</a></h3>
                <h3 class="heading-2 product-text" style="font-size:15px">${{this.price}}</h3>
              </div>
            </div>
          </div>
        {{/each}}
      </div>
    </div>
  </div>


  {{!-- <section class="tiles">
    {{#each prodList}}
      <div class="inner">
        <article class="style3">
          <span class="image">
            <img src="" alt="" />
            <a
              href="javascript:void(0)"
              id="show-detail"
              data-url="/:brand/:id"
            >
              <h2>{{this.name}}</h2>

            </a>
          </span>
        </article>
      </div>
      <a href="#my-dialog">Edit</a>

      <div class="modal" id="my-dialog">
        <div class="modal-dialog">
          <form action="/edit/{{this.id}}">
            <input type="hidden" name="product_type" value="{{product_type}}" />

            <input type="submit" value="Edit" />
          </form>
          <h1>my dialog</h1>
          <!-- Add your modal content here -->
        </div>
      </div>

      <a href="/delete/{{this.id}}?product_type={{product_type}}">Delete</a>
    {{/each}}
  </section> --}}
</div>